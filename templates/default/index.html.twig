{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
<style>
   
</style>


<div class="sidebar">
   
</div>

<div class="example-wrapper">

    <nav class="navbar navbar-expand-lg ">
      <img src="{{ asset('/uploads/logoWW.png') }}" class="logo" />
      <div class="container">

        <a class="navbar-brand" href="#"><i class="fas fa-bars"></i></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" 
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('index') }}">COURS<span class="sr-only">(current)</span></a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                     EL MARDI
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="#">se deconnecter</a>
                    {# <a class="dropdown-item" href="#">Another action</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="#">Something else here</a>
                    </div> #}
                </li>

            </ul>
        </div>
    </div>
</nav>

</div>
                 {# -------------------------- nav end ------------------------------ #}
<div class="container">    

    <div class="top-bar">
        <div class="row">
           <div class="col-sm-6" style="margin-top: 4px">
              <i class="fas fa-th-large"></i> <span class="Affichage"> Affichage 1-8 de 11 résultats</span>
           </div>
           <div class="col-sm-6" style=" display: flex; justify-content: flex-end;">
              {# <ul>
                <li>#}
              
                    <select class="select-module" placeholde="all"> 
                        <option value="0"> All </option>
                     </select>
                     <input type="text"  placeholder=" chercher ..." class="search">
                     <button type="submit" class="btn-search"> <i class="fas fa-search"></i> </button>
            
                {# </li>
              </ul> #}
              
           </div>
        </div>
    </div>
</div>

          {# --------------------------------------- top bar  ------------------------------------- #}

<div class="container">

    <div class="row contenu-video">

            {% for video in videos %}
                {# <li>{{ user.username}}</li> #}
                <div class="col-sm-3 card-video mt-4" >
                  <a href="{{ path('video', { 'id': video.id }) }}">
                        <img src= {{ "/uploads/" ~ video.image}}    class="thumbnail" >
                        <div >
                        <p style="font-size: 13px; font-weight: bold; margin: 4px; margin-top: 11px"> 
                            {{ video.description}}
                        </p>
                        </div>
                   </a>
                </div>
            {% endfor %}

    </div>

    {# --------------------------------------- row ----------------------------------------   #}
{# 
    <div class="row contenu-video">

            {% for video in videos %}
                <div class="col-sm-3 card-video mt-4" >
                  <a href="{{ path('video', { 'id': video.id }) }}">
                        <img src= {{ "/uploads/" ~ video.image}}    class="thumbnail" >
                        <div >
                        <p style="font-size: 13px; font-weight: bold; margin: 4px; margin-top: 11px"> 
                            {{ video.description}}
                        </p>
                        </div>
                   </a>
                </div>
            {% endfor %}

    </div> #}

    {# --------------------------------------- row ----------------------------------------   #}

</div>



    
 {# <video width="320" height="240" controls>
  <source src="{{ asset('/uploads/PrBahij.mp4') }}" type="video/mp4">
 </video>  #}
</div>


{% endblock %}
